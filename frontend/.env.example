# Backend API base URL — host only, NO /api suffix (paths include /api/...).
# Same-domain docker compose: leave empty (nginx proxies /api).
# Split Railway/Vercel: https://your-backend.up.railway.app
VITE_API_BASE=

# SIT | UAT | PROD
VITE_APP_ENV=SIT
